{
  "name": "learn-nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_OPTIONS='--openssl-legacy-provider --inspect' next dev",
    "build": "cross-env NODE_OPTIONS='--openssl-legacy-provider --inspect' next build",
    "start": "cross-env NODE_OPTIONS='--openssl-legacy-provider --inspect' next start",
    "lint": "cross-env NODE_OPTIONS='--openssl-legacy-provider --inspect' next lint"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "next": "11.1.2",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@types/react": "17.0.20",
    "eslint": "7.32.0",
    "eslint-config-next": "11.1.2",
    "typescript": "4.4.3"
  }
}
